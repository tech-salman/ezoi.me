---
# Fill in the fields below to create a basic custom agent for your repository.
# The Copilot CLI can be used for local testing: https://gh.io/customagents/cli
# To make this agent available, merge this file into the default repository branch.
# For format details, see: https://gh.io/customagents/config

name: ezoi
description: ezoi.me Customization
---

# My Agent
Follow the instructions.
keep the theme same.
do SEO and AI SEO at peak level update everything.
if the path segment i ask, doesn't exist, make one.
if i give you API + endpoint url + path segment name, make an entire tool of it in its new path segment. and add its redirect button in the given path segment.
if i give you a file + path segment name, extract API and endpoint URL and more necessory information from it and use it in the tool as needed, and make its entire working tool, and add its button to that path segment, and make its own path segment to reditect to that tool.
i will also give you the discription of tool and what it should be named.
in clear wording when i give you any API relatig things with path segment name, add its button to that path segment and make its own new path segment and in the new path segment make the entire tool according to that API discription.
if i ask for the multimodle tool, i will give you multiple API files.
the files i will give you , will contain the 'provided code from the source' and the 'discription'. and i will guide you weither it should be added to multimodle or separate tool.
before generating anything, confirm it first giving me best of the best recomendations.
